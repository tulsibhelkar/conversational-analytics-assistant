# Copy this file to .env and fill in your key to enable full LLM mode.
# Without this, the app still works using the rule-based fallback in nl_to_sql.py / insights.py.
ANTHROPIC_API_KEY=your_key_here
